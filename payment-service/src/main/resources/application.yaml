server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: payment-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/microservices_platform?currentSchema=payment_service}
    username: ${SPRING_DATASOURCE_USERNAME:payment_service_app}
    password: ${SPRING_DATASOURCE_PASSWORD:payment_service_secret}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  security:
    user:
      name: ${ACTUATOR_USERNAME:actuator}
      password: ${ACTUATOR_PASSWORD:actuator-secret}

app:
  security:
    jwt-secret: ${JWT_SECRET:dev-jwt-secret-change-in-prod}
    gateway-secret: ${GATEWAY_SECRET:dev-secret}

management:
  metrics:
    distribution:
      percentiles-histogram:
        http.server.requests: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized
  info:
    env:
      enabled: true

info:
  app:
    name: payment-service
    description: Payment processing service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true
    metadata-map:
      user.name: ${spring.security.user.name}
      user.password: ${spring.security.user.password}
